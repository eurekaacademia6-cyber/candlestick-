# Build checklist

1. Open the repository root in Android Studio.
2. Use JDK 17.
3. Let Gradle sync.
4. Build `app:assembleDebug`.

The included GitHub Actions workflow performs the same build with Gradle 8.13 and Android SDK 36.
